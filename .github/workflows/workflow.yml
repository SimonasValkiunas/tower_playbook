name: My Workflow
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      # - name: Checkout code
      #   uses: actions/checkout@v2
      - name: Build app
        run: |
          ansible all -i hosts -m ping
